import SmartCal, { isValidExpression } from "smartcal";

export const isValidCondition = isValidExpression;
export const evaluateCondition = SmartCal;
export const isValidFormula = isValidExpression;
export const evaluateFormula = SmartCal;
